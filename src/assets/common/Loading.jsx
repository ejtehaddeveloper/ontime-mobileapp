/* eslint-disable react/react-in-jsx-scope */
import {ActivityIndicator} from 'react-native';
import {Colors} from '../constants';
const Loading = () => <ActivityIndicator size="large" color={Colors.primary} />;
export default Loading;
